### 7-1. DIDの理想像

DID（Decentralized Identifier）が目指している世界は、非常に美しい。

- 中央管理者に依存しないID
- 自己主権的な鍵管理
- 検証可能な履歴
- グローバルに相互運用可能な識別子

理論上は、
「誰が」「いつ」「どの鍵で」
何を承認したかを、どこでも検証できる世界だ。

この理想像自体を否定する必要はない。
むしろ、方向性としては正しい。

問題は、この理想をそのまま実装しようとしたときの現実にある。

### 7-2. 実装の重さ

DIDを「ちゃんと」実装しようとすると、急に構成要素が増える。

- DID Document
- Verification Method
- Service Endpoint
- 鍵の失効・更新
- 履歴管理
- オフチェーン／オンチェーンの切り分け

これらは一つ一つが正しい設計要素だが、
組み合わせた瞬間に運用が重くなる。

特に問題になるのは、

> 「今、この署名は有効なのか？」

という問いに答えるために、
複数の要素を横断的に確認しなければならない点だ。

実装者にとっては学習コストが高く、
利用者にとっては「よくわからない仕組み」になる。

結果として、
DIDは「正しいが使われない技術」になりやすい。

### 7-3. Resolver問題

DIDの中核にあるのが Resolver だ。

- DID文字列を受け取り
- 対応するDID Documentを取得し
- 検証に必要な情報を返す

理屈はシンプルだが、ここで重要な問題が生じる。

Resolverは誰が運用するのか？
Resolverは常に正しい情報を返すのか？
Resolverが落ちたら検証はどうなるのか？

「分散ID」を名乗りながら、
検証の入り口が事実上の集中ポイントになるケースも多い。

一方、Symbolではこうした問題が起きにくい。

- 公開鍵はアカウントそのもの
- 署名者はトランザクションに必ず残る
- 検証に必要な情報はチェーン上に揃っている

Resolverを挟まなくても、
ブロックチェーン自体が検証基盤になる。

### 7-4. 鍵ローテーションという現実

DID議論で必ず出てくるのが「鍵ローテーション」だ。

- 鍵が漏洩したらどうする？
- 定期的に鍵を交換したい
- 過去の署名はどう扱う？

これは正しい問題提起だが、
現実にはこうした問いは用途依存である。

- ログイン用途なのか
- 承認ログなのか
- 資産操作なのか

すべてのユースケースで
フルスペックの鍵ローテーション機構が必要なわけではない。

Symbolでは、

- アカウントの公開鍵＝主体
- マルチシグによる段階的移行
- 用途ごとに鍵を分離する設計

といった形で、
過剰にならない現実的な選択肢を取ることができる。

### 7-5. 実用に必要な最小条件

ここで、問いを整理し直そう。

実用的な「分散的な承認・認証」に本当に必要なものは何か？

- 誰が署名したかが検証できる
- その署名が改ざんされていない
- いつ行われたかがわかる
- 後から第三者が確認できる

この4点が満たされていれば、
多くの実用システムは成立する。

必ずしも、

- DID文字列である必要はない
- DID Documentを解決する必要もない
- Resolverを常設する必要もない

Symbolはこの最小条件を自然に満たす設計になっている。

### 7-6. 「DIDにならない」という選択

本章で言いたいのは、
「DIDは不要だ」という主張ではない。

むしろ、

> DIDにならなくても、DID的な価値は実現できる

という選択肢が存在する、という整理だ。

Symbolは、

- 署名が第一級市民
- 公開鍵が主体
- 検証がチェーンで完結する

という思想を持っている。

その結果、
「DIDを実装しようとして苦しくなる前に、
もう十分なものが揃っている」
という状態になっている。

### 章のまとめ

DIDは理想であり、設計論である。
Symbolは実装であり、運用である。

理想に寄せることもできるが、
無理に名前を合わせる必要はない。

次章では、
この「署名ドリブンなSymbol」が
具体的に何に使えるのかを見ていく。

## 第8章：署名ドリブンSymbolの使い道

ここまで見てきたように、署名ドリブンな設計は
「ブロックチェーンに何を書くか」よりも
「誰が、いつ、何を承認したかをどう残すか」に焦点を当てる。

この章では、Symbolをアプリケーションの主役としてではなく、
**承認と証跡の基盤（＝L0的存在）**として使ったときに、
どのような設計が可能になるのかを具体例で見ていく。

### 管理画面ログイン

もっとも分かりやすい例が、管理画面へのログインである。

従来の管理画面は、
ID・パスワード・2FA・IP制限などを重ねて守られてきた。
しかし本質的には「この操作を行っているのは誰か」を
完全には保証できていない。

署名ドリブンなログインでは発想が逆になる。

- サーバーがチャレンジを発行
- ユーザーが秘密鍵で署名
- 公開鍵（＝Symbolアカウント）で検証

ここでSymbolは「ログイン処理」を行わない。
署名という事実を残すだけだ。

管理者アカウント＝公開鍵であるため、

- 誰がログインしたか
- いつ承認したか
- どの鍵で行ったか

が後から必ず検証できる。
パスワード流出や共有アカウントといった問題は、
設計段階で消滅する。

### 承認ワークフロー

申請・承認・実行という業務フローは、
実は署名と非常に相性が良い。

たとえば、

- 支払い承認
- 設定変更承認
- リリース承認

これらは「処理」ではなく「意思決定」である。

Symbolでは、

- 承認＝署名
- 実行＝オフチェーン処理

と明確に分離できる。

承認結果はトランザクションとして残り、
誰がどの段階でOKを出したかが改ざん不能な形で保存される。
実処理は既存システムで行えばよい。

チェーンは裁定者であり、実行者ではない。
この割り切りが、運用コストと設計の自由度を両立させる。

### NFC / スマホ署名

署名ドリブン設計は、UIを極限まで単純化できる。

NFCやQRコードを使えば、

- チャレンジを受け取る
- 署名する
- 返す

これだけで承認が完了する。

スマホ側に表示されるのは
「承認しますか？」という問いだけでよい。
ID入力も、パスワード入力も存在しない。

Symbolは「誰の署名か」を公開鍵で即座に特定できるため、
**“かざして承認”**のような体験を、
特別な中央認証サーバなしで実現できる。

### IoT / 物理鍵

IoTや物理鍵の世界では、
「オンライン前提」「常時接続」が大きな制約になる。

署名ドリブンSymbolでは、

- デバイスは署名するだけ
- 検証と記録は後からでも可能

という設計が取れる。

ドアの解錠、設備の起動、モード変更などを
「署名イベント」として扱えば、

- 誰が操作したか
- 正規の鍵だったか
- 後から検証できるか

をチェーン上で保証できる。

物理世界の操作に、改ざん不能な署名ログを与える。
これだけで、セキュリティモデルが一段階上がる。

### 監査・証跡用途

最後に、もっとも地味だが強力な用途が監査である。

Symbolに残るのは、

- 状態ではなく
- 判断でもなく
- 署名という事実

これが重要だ。

「何が起きたか」ではなく
「誰が責任を持って承認したか」が残るため、
後からの説明責任が極めて明確になる。

ログは削除できず、順序も保証される。
監査用DBを別途設計する必要すらない。

### 小まとめ

ここで紹介した用途に共通しているのは、
Symbolがアプリケーションの一部ではなく、
すべてのレイヤーの下に静かに存在している点である。

Symbolは「何でもできるチェーン」ではない。
しかし「署名を基準に世界を組み立てる」という一点において、
極めて強力な基盤になる。

次章では、この立ち位置を一段抽象化し、
Symbolを L0（レイヤーゼロ） として捉え直す。
