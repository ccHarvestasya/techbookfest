## 第4章：最小構成で作る署名ログイン

― チェーンに頼りすぎない、しかし嘘をつかない ―

> この章では、Symbol を使った署名ログインを  
> **「最小構成」「オフチェーン検証」「実用 UX」** という制約の中で組み立てる。  
> ここで扱うのは“完成形”ではなく、成立条件を満たす最小単位である。

### 4.1 全体構成図（Browser / Wallet / Server / Symbol）

#### 4.1.1 登場人物は4つだけ

まず、構成要素を極限まで削る。

- Browser
  ユーザー操作の起点。秘密鍵は持たない。
- Wallet
  署名だけを行う存在。判断はしない。
- Server
  チャレンジを発行し、署名を検証する。
- Symbol
  公開鍵の取得元（≒ 信頼できる事実の参照先）

👉 スマートコントラクトも、オンチェーン認証も出てこない。

#### 4.1.2 基本フロー（文章で図を描く）

1. Browser が Server に「ログインしたい」と伝える
1. Server が チャレンジを生成して返す
1. Browser が Wallet に署名を依頼する
1. Wallet が署名を返す
1. Server が オフチェーンで検証する
1. 成立すればセッションを発行する

ここで重要なのは：

> チェーンは一度も“実行”に関与しない

Symbol は「事実を問い合わせる場所」としてのみ使われる。

### 4.2 チャレンジ生成 ― 署名ログインの背骨

#### 4.2.1 なぜチャレンジが必要か

署名ログインは「鍵を持っていること」の証明であり、
その瞬間に意思を示したことを示す必要がある。

それを担保するのがチャレンジである。

#### 4.2.2 チャレンジの要件

最低限、以下を満たす必要がある。

- 十分なランダム性
  - UUID v4
  - 256bit ランダム値 など
- 一度きり
  - 使い回し不可
- 有効期限
  - 数十秒〜数分

```plaintext
challenge = {
  nonce: "random-string",
  expiresAt: 2026-01-19T12:34:56Z
}
```

#### 4.2.3 署名対象は「文字列」でよい

- JSON でも
- プレーンテキストでも

重要なのは 署名対象が曖昧でないこと。

### 4.3 ウォレット署名 ― UXと現実解

#### 4.3.1 署名方法は3系統ある

- QRコード
  - PC × スマホの王道
  - 視覚的にわかりやすい
  - カメラ権限が必要
- DeepLink
  - モバイル完結
  - 失敗時のフォールバックが重要
- 手動署名
  - 開発者向け

CLI / 拡張ウォレット想定

👉 どれを選んでも、署名結果は同じ

#### 4.3.2 UX上の注意点（かなり重要）

- 何に署名しているかを見せる
- 金銭トランザクションと混同させない
- 「ログイン用署名です」と明示する

Symbol はトランザクション署名に慣れているユーザーが多い。
心理的ハードルを下げる説明が必要。

#### 4.3.3 ウォレットは「判断しない」

ウォレットの役割は一つだけ：

> 与えられたメッセージに署名する

ログイン可否の判断、意味解釈は すべてサーバ側。

### 4.4 オフチェーン検証 ― 本当に大事な部分

#### 4.4.1 公開鍵の取得

Symbol の強みがここで活きる。

- アカウント ＝ 公開鍵
- REST で取得可能
- 署名者が必ず特定できる

👉 DID レジストリを自作しなくてよい。

#### 4.4.2 署名検証フロー

1. チャレンジが未使用か
1. 有効期限内か
1. 署名が正しいか
1. 公開鍵と一致するか

すべてオフチェーンで完結。

#### 4.4.3 REST / WebSocket の使い分け

- REST
  - 単発ログイン
  - 実装が簡単
- WebSocket
  - 状態変化を追いたい場合
  - 後章で扱うオンチェーン連携の布石

👉 この章では REST で十分。

### 4.5 ログイン成立条件 ― どこからが「本人」か

#### 4.5.1 「本人」とは何か

この仕組みで言えるのは、ただ一つ。

> その公開鍵に対応する秘密鍵を、その瞬間に操作した

それ以上でも、それ以下でもない。

- 実名はわからない
- 人間かどうかもわからない
- しかし「なりすましではない」

#### 4.5.2 セッション管理は従来技術でよい

- Cookie
- JWT
- Server-side session

ブロックチェーンは一切不要。

#### 4.5.3 ここまでで「何が残っていないか」

- ログイン履歴はチェーンにない
- 承認事実もチェーンにない
- 失効情報もない

👉 では、何を残すべきか？

次章へのつながり

第4章では「成立する署名ログイン」を作った。

しかしそれは、すべてが揮発性でもある。

次の章では問いを反転させる。

「オンチェーンに、何を残すと価値が生まれるのか」
