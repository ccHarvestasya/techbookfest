\documentclass[11pt]{article}

% --- 日本語（LuaLaTeX） ---
\usepackage{luatexja}
\usepackage{luatexja-fontspec}
\setmainfont{Noto Serif CJK JP}
\setsansfont{Noto Sans CJK JP}
\setmonofont{Noto Sans Mono CJK JP}

% --- レイアウト ---
\usepackage[a4paper,margin=25mm]{geometry}
\usepackage{setspace}
\setstretch{1.12}

% --- 画像/リンク ---
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=black
}

% --- 見出し/段落 ---
\usepackage{titlesec}
\titleformat{\section}{\Large\bfseries}{}{0em}{}
\titleformat{\subsection}{\large\bfseries}{\thesubsection}{1em}{}
\newcommand{\sectionbreak}{\clearpage}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}

% --- コードブロック（Pandocが自動で使うことが多い） ---
\usepackage{fancyvrb}
\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,commandchars=\\\{\}}

% --- ノンブル（ページ番号） ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % 全部クリア
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\thepage}

% --- Pandoc 生成物の互換（tightlist / Shaded） ---
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}%
}

\usepackage{tikz}
\usepackage{transparent}
\usepackage{xcolor}
\usepackage{framed}
\definecolor{shadecolor}{RGB}{245,245,245}
\newenvironment{Shaded}{\begin{shaded}}{\end{shaded}}

% --- タイトル情報（Pandoc変数） ---
\newcommand{\BookTitle}{$title$}
\newcommand{\BookSubtitle}{$if(subtitle)$$subtitle$$endif$}
\newcommand{\BookAuthor}{$author$}
\newcommand{\BookCircle}{$if(circle)$$circle$$endif$}
\newcommand{\BookVersion}{$if(version)$$version$$endif$}
\newcommand{\BookDate}{$if(date)$$date$$endif$}
\newcommand{\BookRights}{$if(rights)$$rights$$endif$}
\newcommand{\BookContact}{$if(contact)$$contact$$endif$}
\newcommand{\BookRepo}{$if(repository)$$repository$$endif$}

% --- Pandocが本文を流し込む前提の最低限 ---
\begin{document}

% --- before-body（Pandocの版差吸収：両方書く） ---
$for(include-before)$
$include-before$
$endfor$
$for(include-before-body)$
$include-before-body$
$endfor$

$body$

% --- after-body（Pandocの版差吸収：両方書く） ---
$for(include-after)$
$include-after$
$endfor$
$for(include-after-body)$
$include-after-body$
$endfor$

\end{document}

